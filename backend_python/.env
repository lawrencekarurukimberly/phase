# backend/.env for Python FastAPI application
# This file contains environment variables for your backend.
# IMPORTANT: Do NOT commit this file to your public Git repository.

# Port for the FastAPI server to run on
PORT=8000 # FastAPI typically runs on port 8000 by default

# PostgreSQL Database Connection URL
# Replace 'petpals_user', 'hakuna', 'localhost:5432', and 'petpals_db' with your actual database credentials.
DATABASE_URL="postgresql://petpals_user:hakuna@localhost:5432/petpals_db"

# You might add other environment variables here as your project grows,
# e.g., for API keys, secret keys, etc.